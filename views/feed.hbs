<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<title>Document</title>
</head>
<body>
	<article class="feed">
	<ul>
	{{#each items}}
		<li data-uuid="{{id}}">
			<img alt="Picture of {{subject.name}}" src="images/blanco.png" data-src="{{ image.url }}" width="300" height="450">
			<header>
				<img alt="" class="avatar" src="{{ subject.avatar.url }}"> 
				<h2>{{ subject.name }}</h2>
			</header>
			<h1><a href="appearance/{{id}}">{{title}}</a></h1>
		</li> 
    {{/each}}
</ul>

</article>
</body>
</html>">